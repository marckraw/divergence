---
"divergence": minor
---

Fix CI chaperone execution by installing the standalone release binary directly from GitHub Releases instead of using `bunx`. Add SHA256 verification during download for integrity.
