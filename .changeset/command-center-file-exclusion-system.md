---
"divergence": minor
---

Add configurable command center file exclusions with built-in noise filtering, user-defined exclude patterns, and optional `.gitignore` support. This also adds Settings controls for managing the exclusion list and gitignore behavior.
