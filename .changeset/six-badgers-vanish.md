---
"divergence": patch
---

Simplify the global switcher so `Cmd+K` reveals existing sessions or opens them in a new tab, default tab labels follow the active session, and the command center search stays focused when reopened.
