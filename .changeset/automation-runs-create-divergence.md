---
"divergence": minor
---

Automation runs now create a fresh divergence (branch-isolated clone) before spawning the agent, keeping the base project directory untouched. The new divergence appears in the sidebar after each run.
