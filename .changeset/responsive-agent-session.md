---
"divergence": patch
---

Improve responsive design of agent session conversation view using CSS container queries. The sidebar now shows/hides based on actual container width instead of viewport width, and header, timeline, and composer layouts gracefully adapt to narrow split panes.
