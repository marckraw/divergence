---
"divergence": patch
---

Increase the configurable layout tab limit to default to 20, expose the cap in Settings > General, and show a toast when new tab creation is blocked by the configured limit.
