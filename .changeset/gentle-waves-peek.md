---
"divergence": minor
---

Add a "View" tab to the quick edit drawer for markdown files (.md, .mdx, .markdown) that renders the file content using the same Markdown component used in agent session chat messages, providing a read-only rendered preview alongside the existing Diff and Edit tabs.
