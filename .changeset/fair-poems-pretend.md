---
"divergence": patch
---

Show focused pane changed files from the local working directory instead of branch-to-base diffs, so the Working view matches the repository's current uncommitted changes.
