---
"divergence": patch
---

Improve Command Center responsiveness in large projects by debouncing search input, capping visible results, and reducing result-list animation churn. The modal now also shows when results are truncated so users can narrow their query instead of rendering thousands of matches.
